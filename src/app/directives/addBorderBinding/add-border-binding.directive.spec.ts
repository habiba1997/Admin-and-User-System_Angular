import { AddBorderBindingDirective } from './add-border-binding.directive';

describe('AddBorderBindingDirective', () => {
  it('should create an instance', () => {
    const directive = new AddBorderBindingDirective();
    expect(directive).toBeTruthy();
  });
});
